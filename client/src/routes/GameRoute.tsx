import React, { useState } from 'react';

//import { socket } from '../services/socket';
export const GameRoute = () => {
   

    return (
        <div>
          <h1>Game is here</h1>
        </div>
    );
};